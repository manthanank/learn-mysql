# GitHub Copilot Custom Instructions - learn-mysql

- This project is a TypeScript + Express 5 + MySQL 8 / InnoDB curriculum and interactive SQL sandbox.
- Use ES Module syntax with `.js` extensions on imports.
- Ensure parameterized queries with `?` placeholders are used for all dynamic SQL execution.
- Maintain dual compatibility with both live MySQL pools and offline mock storage.
